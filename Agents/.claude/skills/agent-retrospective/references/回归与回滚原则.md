# 回归与回滚原则

本文件只用于设计候选改动的验证和回滚方式。

- 质量验收使用受影响的历史样本或明确的失败样例，写清基线和通过条件。
- 效率验收至少比较一次可量化指标，例如读取次数、工具调用、耗时或 token；若效率不下降，说明保留成本的原因。
- 先在有限样本上验证，再由人工决定是否扩大使用范围；不能覆盖已完成的项目交付物。
- 回滚方式必须指向可恢复的文件版本或变更集，并写明触发条件，例如质量回退、输入不兼容或成本超过上限。
