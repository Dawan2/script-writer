# 周期 1 · 第 2 波 W2 余量与禁区

| 项 | 值 |
| --- | --- |
| 里程碑 | 周期 1 / 第 2 波 / W2 |
| 槽位 | 计划 · 余量对齐（只出清单与验收口径，不改产品代码） |
| 代码基线 | `main @ abb779e`；核验态为 `main` + 六个集成前置提交（本地临时分支，已删除，未推送） |
| 工作分支 | `cursor/w2-residual-scope-and-forbidden-zones-058e` |
| 产出性质 | 已落地勿重做清单、`31`/`36`/`29` 的缩小后验收、W2 第一批领取结论、与权威裁定的差异登记。不含代码改动，不改他槽文件 |
| 权威编号来源 | `cursor/w1-task-id-adjudication-058e @ d0e97a0`（`W1-任务编号唯一裁定.md`，`C1-W1-01…42`） |
| 排程与实测来源 | `cursor/w2-landing-slices-058e @ fa5d5d1`（`W2-落地切片.md`，14 个任务包） |
| 已落地修复来源 | `cursor/w1-failure-path-contract-mapping-058e`（`6a68305`、`dd10033`）、`cursor/w1-agent-skill-runtime-mapping-058e`、`cursor/w1-product-frontend-mapping-058e` |
| 勘误分支 | 不存在。远端无 `cursor/w2-landing-slices-errata`，全仓含「勘误」字样的提交四条均属其他槽的内部勘误，无一是切片勘误，故无可引用对象 |

裁定与切片在测试基线上给出的数字不同：裁定按「`31` 解 `test:agent` 4 项、`36` 解 `test:api` 4 项」排程，切片实测整合后 `test:agent` 已全绿。本文按裁定第 9.6 条「发现冲突先登记勘误再动手」登记这批差异，并给出工作槽可直接照做的缩小后验收。

**冲突处理原则：编号、归属与文件边界以裁定为准；数字、命令与余量以切片实测为准（本槽已逐条复现）。**

---

## 1. 已落地勿重做清单

六个提交只在分支上，`main` 仍是修复前状态。它们不是「待办的一部分」，而是**已经写完的代码**——对应工作项开工时若照原范围重做，改的是已被修过的行。

| 提交 | 来源分支 | 已落地的行为 | 落在谁的范围里 | 该工作项因此不再做 |
| --- | --- | --- | --- | --- |
| `b53b6c5` | `w1-product-frontend-mapping-058e` | 项目搜索防抖失效、筛选无结果的误导文案、工作台错误提示改用 `role="alert"`（`U-12`/`13`/`14`）。改 `page.tsx`、`project-list.tsx` | `C1-W1-17` / `05` / `01` 的两个热点文件 | 不得重写这三处；`page.tsx` 整体下移 2 行，行号按并入后实际位置定位 |
| `01b9fb7` | `w1-agent-skill-runtime-mapping-058e` | `full_generate/SKILL.md` 两处工具名从试稿阶段名改回「初始化剧本全稿」「检查剧本全稿」（`A-03`） | `C1-W1-29` 的八阶段 `SKILL.md` | 不得重写这两行；清单直接引用修正后的工具名 |
| `16cc256` | 同上 | `script-distillation.test.mjs` 夹具补齐公式字段，恢复验证现行契约 | `C1-W1-31` 的 `test:agent` 完成判定 | 原第 5 项失败已由此解，不再计入 `31` 的收益 |
| `350124c` | 同上 | `CLAUDE.md` 补 `humanizer-zh`、`document-sync`，脚本路径覆盖 `.claude/skills/` 与 `skills/` 两个目录 | `C1-W1-34` 的 `CLAUDE.md` 一致性项 | 该项已完成，`34` 只做其余检查项与防回退 |
| `6a68305` | `w1-failure-path-contract-mapping-058e` | `script_sync_service.py:2113` 的 `utc_now_iso` → `_utc_now_iso`；新增 `apps/api/requirements-dev.txt`；README 补「测试」一节 | `C1-W1-28` 的测试依赖说明 | 不得重写 README 测试一节，只做与之一致的核对与补充 |
| `dd10033` | 同上 | `knowledge_status` 改为如实反映查询结果（`loaded`/`empty`/`unavailable`，保留 `skipped_unresolved_profile`）；四种状态在执行策略文件里各自成文；门禁新增「标记已获取却零条原则时不放行」；`strategyFormulaPayload` 区分两种拒绝原因；新增 `Agents/tests/helpers/knowledge-library.mjs` 最小知识库夹具；`test_script_output_contracts.py` 补 `2.1-world-view.json` 夹具 | `C1-W1-31` 的主体、`C1-W1-36` 的 2 项失败 | `31` 不得重做三态标记、门禁与执行策略成文；`36` 不得重做世界观夹具 |

两个作废的提交，集成时不并入：

| 提交 | 处置 | 依据 |
| --- | --- | --- |
| `12c9ef7`（`w1-agent-intelligence-plan-058e`） | 作废。它对 `script_sync_service.py:2113` 的改动与 `6a68305` **完全相同**（两侧 blob 均为 `7556c68..2d0e986`），取 `6a68305` | 裁定 §7 与切片 §3 同判「第二个作废」，只是取舍方向相反，见 §5 差异 6 |
| `248c1f6`（同上） | 撤回。把 `httpx` 加进 `requirements.txt`，取 `6a68305` 的 `requirements-dev.txt`——`httpx` 只被 `fastapi.testclient` 需要，不属运行时依赖 | 裁定 4.4 |

---

## 2. 本槽复核的实测基线

在 `main` 上按切片 §3 的顺序逐个 `git cherry-pick -x` 上述六个提交，**六次全部无冲突**，随后在该整合态安装 `Agents/` 的 npm 依赖与 `apps/api` 的 `requirements-dev.txt`，实际执行下列命令。切片给出的数字全部复现，无一处需要改数。

| 判据 | 实测值 | 说明 |
| --- | --- | --- |
| `npm run test:agent` | **83 项 0 失败** | 裁定预期的 4 项存量失败已不存在 |
| `npm run test:api` | **454 项 2 失败** | 两项均在 `test_script_output_contracts.py` |
| `npm run test:zdebug` | 19 项 0 失败 | 需 `test:api` 先通过才会被执行到 |
| `npm test` 链条 | 在**第 2 步** `test:api` 中断 | 裁定 4.5 记的「第一步 `test:agent` 非零退出」已失效；链条里**没有 `test:web`** |
| 服务端英文 `detail` 文案 | 43 条 | `rg -o 'detail=f?"[^"]*"' apps/api/app -g '*.py' \| rg -v '[\x{4e00}-\x{9fff}]'` |
| `beforeunload` | 0 处 | `C1-W1-01` 的基线 |
| 非测试 `.mjs` 总数 | 78 个 | `C1-W1-34` 的目标覆盖面 |
| `check` 语法检查覆盖 | 56 个；`check` + `precheck` 递归展开后 73 个；5 个谁都不覆盖 | 未覆盖者：`screenplay-format-validation.mjs`、`screenplay-length.mjs`、`script-artifacts.mjs`、`preference-summary` 的两个脚本 |
| `precheck` 是否被 `check` 串接 | 未串接 | 两者是并列的独立脚本 |
| 无引用方的 reference | 2 份 | `full_generate/references/` 下的 `全稿验收规则.md`、`剧本内容与格式规范.md` |
| `rg` 不带 `--hidden` | 假阴性成立 | 检索一个确实存在的字符串，不带 `--hidden` 输出 0 条、带则 1 条 |
| `pageLoading` 位置 | `main` 上 `:686`，整合后 `:688` | `C1-W1-05` 按实际位置定位 |

两条统计口径，不写清就会被误判：

1. **`check` 覆盖面必须递归展开 `npm run` 子链再统计。** `check` 与 `precheck` 的 `node --check` 字面量去重后只有 66 个，`precheck` 末尾还串了 `world-view:spec-check` 与 `stage-strategy:check` 两个子脚本，展开后才是 73 个。只数字面量会把覆盖面低报 7 个、把未覆盖数高报到 12 个。
2. **`Agents/.claude` 下的检索命令一律带 `--hidden`。** Agent 的全部 Skill 都在隐藏目录下，`rg` 默认跳过。**Agent 侧任何以「检索输出 0 条」为判据的验收，命令不含 `--hidden` 即视为无效判定。**

未执行：`npm run check:web` 与 `next build`（未装 `apps/web` 依赖）；未启动前后端服务、未跑真实 Agent 任务。各包中界面走查类验收仍须由实现槽在运行时取证。

---

## 3. `31` / `36` / `29` 的缩小后验收

### 3.1 `C1-W1-31` 知识库依赖显式化与可回归夹具 —— 主体已落地，降为余量包

`dd10033` 已落地三态标记、四种状态各自成文、门禁不接受假状态、`strategyFormulaPayload` 的两种拒绝原因与最小知识库夹具。**本包不得重做上述任何一项**，只做四条余量：

| # | 余量验收 | 现状与落点 |
| --- | --- | --- |
| 1 | 夹具覆盖三类情形——有标签/无标签、使用公式/不使用公式、原则含通过标准；新增一条原则或公式只改夹具数据，不改测试代码结构 | `knowledge-library.mjs` 目前只够支撑已恢复的用例，覆盖面与「数据驱动」两项都未成立 |
| 2 | 测试不读写任何生产数据库路径，由路径断言或沙箱证明 | 夹具在临时目录建库（`knowledge-library.mjs:75`），但 `stage-execution-spec.mjs:14` 的默认路径仍指向 `../data/workbench.sqlite3`；余量是把「测试绝不落到默认路径」变成断言，而不是靠约定 |
| 3 | 知识库路径与「不可用时的行为」在代码中是显式契约；`unavailable` 计入任务运行记录供运维排查 | **未落地，且落点在 Agent 目录之外**：`agent_runner.py:3406-3407`（提示词里的 `knowledge_summary`）与 `:3864-3865`（`knowledge_strategy_ready` 事件消息）都只判 `knowledge_status == "loaded"`，`empty` 与 `unavailable` 落进同一个 `else`，输出的仍是「剧本标签尚未全部确定」——正是 `dd10033` 在 Agent 侧改掉的那句错误归因。运维在任务运行记录里看到的原因是错的 |
| 4 | 在没有 `data/` 目录的干净检出上 `npm run test:agent` 全绿，且项数不少于 83 项 | 整合态已是 83 项 0 失败，本条是防回退判据，不是待修项 |

**边界提醒**：第 3 条要落地必须改 `apps/api/app/services/agent_runner.py`，而该文件不在本包持有清单内，按裁定 8.2 归 `37`（W4）与 `42`（W3）。本包须就 `:3399-3413` 与 `:3859-3866` 两个行区间单独登记持有，或把该条移交 `42` 一并实现；两者都不做则第 3 条无法验收。

沿用切片 4.7 的禁止事项：不得把生产 `data/workbench.sqlite3` 纳入仓库或测试依赖；不得改知识库表结构；**不得把知识库不可用改成阻断主链路**（裁定 4.1 已定：不阻断，只要求状态真实且可见）；不得实现用户可见的「本次依据」展示（`C1-W1-40`）。

### 3.2 `C1-W1-36` 阶段状态以产物为准 —— 失败项从 4 降为 2，且失败原因已变

| # | 缩小后验收 | 说明 |
| --- | --- | --- |
| 1 | 阶段无法在缺少声明产物的情况下被标为完成 | 不变 |
| 2 | 下游初始化遇到「状态完成但产物缺失」时，说明写清缺哪个产物与下一步做什么，不含文件路径与内部术语，不再出现「不存在或不是有效 JSON」 | 不变 |
| 3 | `npm run test:api` 从 454 项 2 失败降为 0 失败，且不新增失败项 | **不是原来的 4 项**。`dd10033` 补 `2.1-world-view.json` 后已解 2 项 |
| 4 | 字数下限与可选目标语台词两条规则的断言强度不得下降 | 不变，且是本包最容易被绕过的一条 |

**剩余 2 项的现因已经换了**：不再是「世界观产物缺失」，而是夹具工作区里没有执行策略文件，`check-full` 走到 `stage-execution-spec.mjs:690` 返回「执行策略缺失或已失效，请重新调用执行策略工具」。两项用例是 `test_full_generation_normalizes_legacy_trial_episode_headings` 与 `test_trial_and_full_checks_allow_optional_target_dialogue`，本槽已逐项复现，两者的失败原因完全相同。

后一项的断言（`:276-279`）没有传 `msg`，失败输出只有一句 `1 != 0`，看不出原因。本包定位问题时不要只看测试输出，取证时建议把 `check-full` 的 `issues` 一并落进证据。

**修法只有补夹具一条路，禁止削弱断言。** 夹具须在 `runtime/{阶段}/` 下同时产出 `执行规范.md`、`execution-spec.json`、`执行策略.md`、`execution-strategy.json`，并满足门禁的全部条件：`stage` 对应、`执行策略.md` 的 sha256 与快照一致、正文不含「成立原因」、`execution_spec_sha256` 与执行规范一致、`project_fingerprint` 与 `script_profile` 与输入一致、标签已定时 `knowledge_status` 取值在 `loaded`/`empty`/`unavailable` 之内、使用公式的阶段 `formulas_resolved` 为 `true`。

手写这份快照很脆——任一 sha256 或指纹算错就换一条失败。**推荐调用真实的执行策略生成路径**：在不设 `ORCA_SCRIPT_KNOWLEDGE_DB_PATH`、也没有 `data/` 的隔离环境下，生成结果的 `knowledge_status` 是 `unavailable`，而该取值被 `dd10033` 新增的白名单接受。`dd10033` 之前门禁是 `!== "loaded"` 一刀切，这条路走不通——**这个修法是 `dd10033` 打开的**。

**禁区**：`stage-execution-spec.mjs` 由 `C1-W1-31` 首位持有，本包不得改动（裁定 6.2、切片 4.8 同判）。若确需改动，等 `31` 释放后单独登记。

### 3.3 `C1-W1-29` 建立阶段验收清单 —— 代码未动，验收基线缩小三分之二

本包没有任何代码被提前落地，缩小的是验收基线：

| 项 | 原口径 | 缩小后 |
| --- | --- | --- |
| 无引用方 reference 数量 | 6 份（`C1-AG-T01` 记） | **2 份**：`full_generate/references/全稿验收规则.md`、`full_generate/references/剧本内容与格式规范.md`。裁定 4.2 已定、切片 1.4 实测、本槽复现一致 |
| 处置方式 | 未分 | `全稿验收规则.md` 在 `SKILL.md` 的收口步骤里显式引用；`剧本内容与格式规范.md` 删除（是 `_shared/references/剧本格式规范.md` 的严格子集） |
| 另 4 份 | 按 6 份口径会被删掉 | 有真实消费者（含「工具返回值下发阅读指令」这种按需供给知识的正确做法），**不得删除**，登记引用方作为 `C1-W1-34` 的白名单依据 |
| 判定命令 | 未指定 | 必须带 `--hidden`。可复现命令见切片 1.4；不带 `--hidden` 的判定一律无效 |
| `full_generate/SKILL.md` 工具名 | 待修 | 已由 `01b9fb7` 修正，清单直接引用修正后的名称，不重写 |

按 6 份口径执行会删掉 4 份有引用方的能力，同时让 `C1-W1-34` 的「无误报」验收第一次运行就产出 4 条误报——这是 `29` 与 `34` 之间唯一的硬耦合。

---

## 4. W2 第一批仍应领取的包

**八包全派，无一取消。** 用户点名的 `13`/`02`/`11`/`28`/`17`/`29` 六包照原样领取（`28`、`17`、`29` 有范围或验收变更），`31`/`36` 的余量**仍然派发**，但性质从「主体实现」变为「余量收口」。

| 包 | 编号 | 结论 | 相对裁定 6.1 的变更 |
| --- | --- | --- | --- |
| P-01 | `C1-W1-13` 前端测试基座 | 仍派，范围不变 | `npm test` 的断点已从第 1 步移到第 2 步，链条末尾仍**没有 `test:web`**。本包须同时新增 `test:web` 脚本并把它串进根 `npm test`；「完整执行到 `test:web`」这条证据要等 P-07 解掉 `test:api` 的 2 项失败后才能取到，两包须互相知会 |
| P-02 | `C1-W1-02` 服务端错误契约 | 仍派，范围不变 | 基线 43 条已复现；范围含 `dependencies.py` 的 4 条 401 文案 |
| P-03 | `C1-W1-11` 文档写入原子化 | 仍派，范围不变 | 无 |
| P-04 | `C1-W1-28` README 对齐 | 仍派，**范围缩小** | README 的「测试」一节已由 `6a68305` 落地，本包改为核对其与 `requirements-dev.txt` 一致，不重写；仍须补「首次安装」的 `cd Agents && npm install`，并给三份历史 Agent 文档加失效标注（保留原文不删） |
| P-05 | `C1-W1-17` 常驻导航与死代码清理 | 仍派，**验收改三段** | 见下方 |
| P-08 | `C1-W1-29` 阶段验收清单 | 仍派，**基线 6 → 2 份** | 见 3.3 |
| P-06 | `C1-W1-31` 知识库依赖与夹具 | **仍派，降为余量包** | 主体已由 `dd10033` 落地；余量四条见 3.1，其中第 3 条需跨包登记持有 |
| P-07 | `C1-W1-36` 阶段状态以产物为准 | **仍派，失败项 4 → 2 且现因已变** | 见 3.2 |

`C1-W1-17` 的验收命令，裁定 5.1 给的两段判定实测不可用，按切片三段并列执行：

| 段 | 判据 | 为什么缺它不行 |
| --- | --- | --- |
| (a) | `top-nav.tsx` 与 `lib/mock-data.ts` 两个文件都不存在 | `mock-data` 在全仓引用数为 **0**，任何检索命令都抓不到它，只有文件存在性能判定 |
| (b) | `rg -n "top-nav'\|top-nav\"\|\bTopNav\b\|mock-data'\|mock-data\"\|mockData" apps/web/src` 输出 0 条（基线 2 条，两条都在 `top-nav.tsx` 自身内部） | 单靠 (a) 无法证明没有残留引用 |
| (c) | `globals.css` 的两处 `.top-nav` 样式规则（`:900`、`:11061`）一并删除，或在证据里写明保留理由 | 裁定 5.1 的命令 `rg -n 'top-nav\|mock-data' apps/web/src` 实测 3 条，其中 2 条是这两处样式，删掉两个文件后命令仍输出 2 条，验收必然不通过 |

**领取顺序建议**：P-07 与 P-01 优先。P-07 解开 `npm test` 的第 2 步中断，P-01 补上链条末尾缺失的 `test:web`——W2 出口条件第 1 条「`npm test` 完整执行到末尾」由这两包共同决定，其余六包的测试类证据都要等它们。

---

## 5. 与权威裁定的差异

裁定第 9.3、9.4 条要求验收口径与测试基线以裁定为准。下表十条是裁定成文后由切片实测（本槽已复现）推翻或细化的部分，按裁定第 9.6 条在此登记。**编号、归属与文件边界一律不动，动的只是数字、命令与余量。**

| # | 裁定的表述 | 实测 | 本文结论 |
| --- | --- | --- | --- |
| 1 | 4.5：整合后 `test:agent` 4 失败、`test:api` 454 项 4 失败 | `test:agent` 83 项 0 失败、`test:api` 454 项 2 失败 | 取实测。裁定的基线表在这两行上失效 |
| 2 | 6.1：`C1-W1-31` 的收益是「解 `test:agent` 4 项存量失败，越早越好」 | 4 项由 `dd10033` 解、第 5 项由 `16cc256` 解 | `31` 不再承担这项收益，降为余量包（3.1）。「越早越好」的理由随之消失，但仍属第一批 |
| 3 | 6.1：`C1-W1-36` 解 `test:api` 4 项，覆盖试稿与全稿的字数下限与可选目标语台词两条规则 | 2 项，且失败原因已从「世界观产物缺失」变为「夹具缺执行策略文件」 | `36` 解 2 项；两条规则的断言强度要求保留（3.2） |
| 4 | 6.4 #2：`test:agent` 从 5 项降至 0 由 `31` 解 4 项 + 集成槽并入 `16cc256` 解 1 项 | 由 `dd10033` 解 4 项 + `16cc256` 解 1 项 | 出口值不变（0 失败），承接者从工作项改为集成前置。**出口条件的数字不用改，改的是谁来兑现** |
| 5 | 6.4 #9 与 §7：知识库门禁不再按状态字面值放行，由 `31` 落地 | `dd10033` 已落地 | W2 对这条的要求改为**保持不回退**，并由 `31` 的余量补齐夹具覆盖面、路径隔离与运行记录透出 |
| 6 | §7 的六项前置取 `12c9ef7`（`NameError`）与 failpath 分支的 `requirements-dev.txt`，**未列 `dd10033`** | 切片六项取 `6a68305` 与 `dd10033` | 提交层面取切片：`6a68305` 一个提交同时包含 `NameError` 修复与 `requirements-dev.txt`，其 `NameError` 改动与 `12c9ef7` 的 blob 完全相同，因此裁定 4.4「取 `requirements-dev.txt`、撤回 `248c1f6`」的实质完整保留。裁定漏列 `dd10033` 是差异 1–5 的根因 |
| 7 | 5.1：`C1-W1-17` 的判定 (b) 为 `rg -n 'top-nav\|mock-data' apps/web/src` 输出 0 条 | 3 条，删掉两个文件后仍 2 条 | 取切片三段判定（§4 表）。裁定 5.1 该命令不可判定 |
| 8 | 6.4 #8：`npm run check` 覆盖全部 78 个 `.mjs` 并包含契约检查 | 78 个总数成立；当前 `check` 覆盖 56 个、并 `precheck` 展开后 73 个、5 个谁都不覆盖、`precheck` 未被 `check` 串接 | 目标值 78 不变，补上基线与「`precheck` 须被串接」这条缺失要求（属第二批 `C1-W1-34`） |
| 9 | 9.1：废号 `C1-W1-29…32` 一律不是领取单位 | 切片 §2 把四个废号「作为别名保留，只用于回溯」 | **取裁定。** 分支名、提交信息、状态登记与验收证据里一律不得出现废号；只有回溯段落可提及。避免两套编号并存后再次出现同号两义 |
| 10 | 4.5：`npm test` 第一步 `test:agent` 非零退出，链条中断 | 断点已移到第 2 步 `test:api`；链条里从来没有 `test:web` | 取实测。这决定了 P-01 与 P-07 的先后（§4 末） |

裁定未受影响、继续有效的部分：`C1-W1-01…42` 的编号与归属、五项废号能力的并入去向、第 0 梯队为 `29`/`31`/`36`、`C1-W1-30` 排第 1 梯队、`TA-04` 并入 `16`、无引用 reference 取 2 份、Skill 计数写「创作主链路 14 个 + 后台知识 11 个」、第 8 节的全部互斥队列、第 6.2 节的两处范围切分。

---

## 6. 禁区

同一时间同一文件只允许一个工作槽持有。下表只列本文新增或收紧的条目，其余沿用裁定第 8 节。

| 文件 / 行区间 | 已落地内容 | 谁不得动 | 持有者 |
| --- | --- | --- | --- |
| `stage-execution-spec.mjs`（三态、门禁、成文、公式拒绝原因） | `dd10033` | `C1-W1-31` 不得重做；`C1-W1-36` 不得改动本文件 | `31` → `30` → 后续 |
| `Agents/tests/helpers/knowledge-library.mjs` | `dd10033` 建立最小夹具 | 只允许扩充覆盖面，不得替换现有夹具结构 | `31` |
| `test_script_output_contracts.py` 的 `2.1-world-view.json` 夹具 | `dd10033` | `C1-W1-36` 不得重做世界观夹具，只补执行策略文件 | `36` |
| `full_generate/SKILL.md` 的两处工具名 | `01b9fb7` | `C1-W1-29` 不得重写 | `29` |
| `Agents/CLAUDE.md` 的 Skill 清单与脚本路径 | `350124c` | `C1-W1-34` 不得重做这一项 | `34` |
| `README.md` 的「测试」一节 | `6a68305` | `C1-W1-28` 不得重写，只做一致性核对 | `28` |
| `page.tsx`、`project-list.tsx` 的搜索防抖 / 空态文案 / `role="alert"` 三处 | `b53b6c5` | `C1-W1-17`、`05`、`01` 均不得重做 | 队列首位为 `17` |
| `agent_runner.py:3399-3413`、`:3859-3866` | **未落地**，`empty`/`unavailable` 仍被表述为「剧本标签尚未全部确定」 | `C1-W1-31` 若要兑现余量第 3 条，须就这两个行区间单独登记持有 | 裁定 8.2 归 `37`（W4）、`42`（W3） |

---

## 7. 本文不做

| 不做 | 原因 |
| --- | --- |
| 不代为 cherry-pick 六个提交 | 属集成槽动作。本槽只在临时分支上验证顺序可行与实测基线，未推送、已删除 |
| 不改裁定、切片与上游各份文档 | 并发修改的最高冲突点。本文只登记差异，落笔由各文档持有槽或集成槽执行 |
| 不新增或改写任何工作项编号 | `C1-W1-01…42` 是唯一领取编号 |
| 不改产品代码 | 本槽定位是余量对齐与验收口径 |
| 不裁定第二、三批的排程变更 | 只在差异 8 上登记 `C1-W1-34` 的基线，排程沿用裁定 6.3 与切片 4.1 |
| 不引用勘误分支 | 远端不存在切片勘误分支，无可引用对象 |
