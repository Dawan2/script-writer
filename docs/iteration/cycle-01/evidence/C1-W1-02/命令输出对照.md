# A-6 / A-7 / C-1 / C-2 命令输出对照（改前 / 改后）

命令与基线出处：实现规格第 6 节。改前一列在 main @ abb779e 上复核，改后一列在本分支上执行。

## A-6　英文 detail 文案：43 → 0

```
$ rg -o 'detail=f?"[^"]*"' apps/api/app -g '*.py' | rg -v '[\x{4e00}-\x{9fff}]' | wc -l
改前：43
改后：0
```

改前 43 条的分布（文件 / 条数）：

```
     16 apps/api/app/services/workspace_service.py
     11 apps/api/app/services/agent_runner.py
     10 apps/api/app/routers/projects.py
      4 apps/api/app/dependencies.py
      1 apps/api/app/routers/auth.py
      1 apps/api/app/routers/agent.py
```

## A-7　子进程输出直送用户：13 → 0

```
$ rg -n 'detail\s*=\s*[^=].*(stderr|stdout)' apps/api/app -g '*.py' | wc -l
改前：13
改后：0
```

改前 13 处清单：

```
apps/api/app/services/workspace_service.py:762:        detail = result.stderr.strip() or result.stdout.strip() or "发行任务书更新失败"
apps/api/app/services/workspace_service.py:810:        detail = process.stderr.strip() or process.stdout.strip() or "project_init 任务书补全失败"
apps/api/app/services/workspace_service.py:1079:        detail = process.stderr.strip() or process.stdout.strip() or "国家-locale 契约解析失败"
apps/api/app/services/workspace_service.py:1358:        detail = result.stderr.strip() or result.stdout.strip() or "阶段确认失败"
apps/api/app/services/workspace_service.py:2352:        detail = detail.strip() or result.stderr.strip() or result.stdout.strip() or "剧本名称同步失败"
apps/api/app/services/workspace_service.py:3759:            detail=result.stderr.strip() or result.stdout.strip() or "Agent project initialization failed",
apps/api/app/services/system_agent_evolution_service.py:515:        detail = (result.stderr or result.stdout or f"exit code {result.returncode}").strip()
apps/api/app/services/script_library_service.py:373:            detail = (result.stderr or result.stdout or "文档转换失败").strip()
apps/api/app/services/script_library_service.py:403:        detail = converted.stderr or textutil.stderr.decode("utf-8", errors="replace") or "DOC 文件无法读取"
apps/api/app/services/preference_summary_service.py:311:        detail = (result.stderr or result.stdout or f"exit code {result.returncode}").strip()
apps/api/app/services/novel_analysis_admission.py:63:            detail=_tool_message(result.stderr or result.stdout, "小说原文暂时无法读取，请检查后重试。"),
apps/api/app/services/memory_sync_service.py:1006:            detail = (process.stderr.strip() or process.stdout.strip() or f"{stage} check failed")[-2000:]
apps/api/app/services/ai_skill_runner.py:113:        detail = (result.stderr or result.stdout or f"exit code {result.returncode}").strip()
```

按规格 3.4 分两档处理：10 处 `HTTPException` 改用注册表错误码、原始输出转入 `root_cause`；
`system_agent_evolution_service.py:515`、`preference_summary_service.py:311`、`ai_skill_runner.py:113`
这 3 处走后台任务通道、不产生 HTTP 响应，只把英文兜底 `exit code N` 改中文并把输出转为写日志，不套信封。

改后同一命令为 0 条，且英文兜底文案也已清零：

```
$ rg -n 'exit code \{' apps/api/app -g '*.py' | wc -l
0
```

## C-1　错误文案字符串匹配：5 行 → 4 行

```
$ rg -n 'message\.includes\(' apps/web/src
改前：5 行
apps/web/src/components/workspace/agent-panel.tsx:553:    if (event.message.includes("Agent 任务已启动")) return "正在准备执行";
apps/web/src/components/workspace/agent-panel.tsx:554:    if (event.message.includes("计划执行")) return "已确认执行计划";
apps/web/src/components/workspace/agent-panel.tsx:555:    if (event.message.includes("正在开始处理当前内容") || event.message.includes("正在继续处理当前内容")) return "正在连接创作服务";
apps/web/src/components/workspace/agent-panel.tsx:556:    if (event.message.includes("已同步当前阶段进度")) return "正在同步阶段进度";
apps/web/src/components/home/landing-page.tsx:683:      setLoginError(message.includes("Incorrect username or password") ? "账号或密码不正确" : message);

改后：4 行
apps/web/src/components/workspace/agent-panel.tsx:553:    if (event.message.includes("Agent 任务已启动")) return "正在准备执行";
apps/web/src/components/workspace/agent-panel.tsx:554:    if (event.message.includes("计划执行")) return "已确认执行计划";
apps/web/src/components/workspace/agent-panel.tsx:555:    if (event.message.includes("正在开始处理当前内容") || event.message.includes("正在继续处理当前内容")) return "正在连接创作服务";
apps/web/src/components/workspace/agent-panel.tsx:556:    if (event.message.includes("已同步当前阶段进度")) return "正在同步阶段进度";
```

剩余 4 行全在 `agent-panel.tsx`，归 `C1-W1-16`（W3）。GATE-02 在本波不归零。

## C-2　兜底文案夹带 HTTP 状态码：2 → 0

```
$ rg -n '\$\{(response\.)?status\}' apps/web/src/lib
改前：2 条
apps/web/src/lib/api-client.ts:72:  return `请求失败（${status}）`;
apps/web/src/lib/admin-api.ts:43:    throw new Error(payload.detail || payload.message || `Request failed: ${response.status}`);

改后：0 条
```
