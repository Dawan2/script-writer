# Wave-01 就绪任务队列（Ready Tasks）

> **追加约定**：本文件按计划槽分区维护。每个槽的内容包裹在
> `<!-- BEGIN:Px -->` / `<!-- END:Px -->` 标记之间；后续槽**只在文件末尾追加自己的分区**，
> 不修改、不覆盖其他分区的有效内容。对已有分区的勘误须由原槽负责人以追加"修订记录"小节完成。

---

<!-- BEGIN:P2 -->
## P2 交互可靠性（Interaction Reliability）

- 来源：`docs/wave-01/P2-interaction-reliability.md`（含每个任务的完整目标/范围/验收/证据/依赖明细，本表为索引）
- 产出分支：`cursor/w1-p2-interaction-reliability-a3c2`
- 状态图例：`ready`＝依赖一就绪即可开工；`blocked`＝等待前置任务或其他槽产出

| 任务 ID | 名称 | 目标（一句话） | 验收要点 | 证据类型 | 依赖 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| W1-P2-T01 | 统一请求层 | 全项目唯一网络出口：分级超时、策略表重试、取消、乱序丢弃、错误信封 | 超时/退避/乱序丢弃/卸载取消全部有单测；裸 fetch 被 Lint 拦截 | 单测 + CI 记录 + Lint 演示 | P1 脚手架 | ready（待脚手架） |
| W1-P2-T02 | 幂等键端到端 | 所有变更请求幂等，重试不产生重复副作用 | 同键并发仅 1 条记录；断网重试不重复创建 | 集成测试 + 故障注入 e2e | T01、服务端脚手架 | blocked(T01) |
| W1-P2-T03 | 乐观更新与快照回滚 | 白名单乐观更新，失败回滚不误伤后续变更 | 双乐观更新竞态测试；连点合并为一次写 | 竞态单测 + 录屏 | T01 | blocked(T01) |
| W1-P2-T04 | 向导会话中断恢复 | 向导每步服务端持久化，刷新/断网/换设备可恢复 | 第 3 步刷新可恢复；断网填写回线同步；双端冲突有选择 UI | e2e（offline 模拟）+ 录屏 | T01、T02 | blocked(T02) |
| W1-P2-T05 | 版本化写与 409 冲突 UX | If-Match 版本写杜绝丢失更新，冲突三选一处理 | 陈旧写被 409 拒；双标签冲突对话框三选项各自正确 | 集成测试 + 双标签 e2e 录屏 | T01 | blocked(T01) |
| W1-P2-T06 | 会话一致性 | 401 单飞刷新重放、不丢稿登录墙、多标签会话广播 | 并发 401 仅 1 次刷新；重登后草稿保住；跨标签登出即时生效 | 单测 + e2e 录屏 | T01 | blocked(T01) |
| W1-P2-T07 | Agent run/step 失败路径 | run/step 持久化状态机：步级重试续跑、畸形输出防御、SSE 续传、触发幂等 | 失败步单独重试且前序不重跑；畸形 JSON 不崩编排器；关页面不丢 run；eventSeq 连续 | 故障注入集成测试 + e2e 录屏 + 事件日志 | T01、T02、Agent 编排（他槽） | blocked(T02) |
| W1-P2-T08 | 批量部分失败与 Saga 清扫 | 批量 207 逐项结果、多步写可补偿、半成品可回收 | 3/10 失败逐项准确且仅重试失败项；注入崩溃后清扫回收 | 故障注入集成测试 + 清扫日志 | 服务端脚手架 | ready（待脚手架） |
| W1-P2-T09 | 离线检测与 Outbox | 三态连接状态机、持久化 outbox 重放、保存指示器 | 断网编辑回线自动重放且数据一致；重放冲突走 T05 流程 | e2e 录屏 + outbox 快照 | T01、T03、T05 | blocked(T03) |
| W1-P2-T10 | 可靠性测试设施 | 故障注入代理、竞态测试工具、offline 预设、Lint 强制 | 注入可编程控制；Lint 违例 CI 报错；T01 用例跑通 | CI 配置与记录 + 反例输出 | T01（同步启动） | ready（待脚手架） |

**建议开工顺序**：T01 + T10 同步起（互相验证）→ T02/T03/T05 并行 → T04/T06/T08 → T07（等 Agent 编排就绪）→ T09。

**后续波次规格（本波不实现，规格见主文档 §4）**：S1 多人实时协作编辑（依赖 T05/T07）、S2 Agent 断点续跑与结果暂存箱（依赖 T05/T07）、S3 离线优先创作模式（依赖 T05/T09）。
<!-- END:P2 -->
