# SW-E034 没有可导出的内容

> **生成物，请勿手改**：本页由 `scripts/gen-error-docs.ts` 从 `src/app/errors/registry.ts` 生成；修改文案请改注册表后运行 `npm run gen:errors`。CI 的 `npm run lint:errors` 会拦截漂移与手改。

| 项目 | 内容 |
| --- | --- |
| 段位 | SW-E03x 输入校验 |
| 发生了什么 | 没有可导出的内容 |
| 原因 | outline.md 缺失或全空白，且 scenes/ 下没有任何场文件——导出已中止，未产生任何文件。 |
| 怎么办 | 先运行 `sw outline` 补大纲，或运行 `sw draft 010 --title "开场"` 创建第一场，然后再导出。 |
| 退出码 | 1（SPEC-03-EXT：运行期错误） |

文案中的 `{key}` 为运行时上下文占位符，输出时由实际值替换。

## 示例输出

```text
✖ SW-E034 没有可导出的内容
  原因：outline.md 缺失或全空白，且 scenes/ 下没有任何场文件——导出已中止，未产生任何文件。
  怎么办：先运行 `sw outline` 补大纲，或运行 `sw draft 010 --title "开场"` 创建第一场，然后再导出。
  详情：https://github.com/Dawan2/script-writer/blob/main/docs/errors/SW-E034.md
```
