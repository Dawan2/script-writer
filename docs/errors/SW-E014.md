# SW-E014 项目自检未通过（{count} 个红项）

> **生成物，请勿手改**：本页由 `scripts/gen-error-docs.ts` 从 `src/app/errors/registry.ts` 生成；修改文案请改注册表后运行 `npm run gen:errors`。CI 的 `npm run lint:errors` 会拦截漂移与手改。

| 项目 | 内容 |
| --- | --- |
| 段位 | SW-E01x 项目 / 文件系统 |
| 发生了什么 | 项目自检未通过（{count} 个红项） |
| 原因 | sw doctor 发现需要处理的问题：{findings}。 |
| 怎么办 | 按报告各红项的「修复」命令逐项处理后重跑 `sw doctor`。 |
| 退出码 | 1（SPEC-03-EXT：运行期错误） |

文案中的 `{key}` 为运行时上下文占位符，输出时由实际值替换。

## 示例输出

```text
✖ SW-E014 项目自检未通过（2 个红项）
  原因：sw doctor 发现需要处理的问题：项目文件、目录布局。
  怎么办：按报告各红项的「修复」命令逐项处理后重跑 `sw doctor`。
  详情：https://github.com/Dawan2/script-writer/blob/main/docs/errors/SW-E014.md
```
