# SW-E031 模板不存在：{templateId}

> **生成物，请勿手改**：本页由 `scripts/gen-error-docs.ts` 从 `src/app/errors/registry.ts` 生成；修改文案请改注册表后运行 `npm run gen:errors`。CI 的 `npm run lint:errors` 会拦截漂移与手改。

| 项目 | 内容 |
| --- | --- |
| 段位 | SW-E03x 输入校验 |
| 发生了什么 | 模板不存在：{templateId} |
| 原因 | templates/ 下没有名为 {templateId} 的模板目录。 |
| 怎么办 | 可用模板：{available}。换用 `--template <id>`，或省略该旗标（默认跟随脚本类型）。 |
| 退出码 | 1（SPEC-03-EXT：运行期错误） |

文案中的 `{key}` 为运行时上下文占位符，输出时由实际值替换。

## 示例输出

```text
✖ SW-E031 模板不存在：nope
  原因：templates/ 下没有名为 nope 的模板目录。
  怎么办：可用模板：short-video。换用 `--template <id>`，或省略该旗标（默认跟随脚本类型）。
  详情：https://github.com/Dawan2/script-writer/blob/main/docs/errors/SW-E031.md
```
