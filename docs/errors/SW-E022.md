# SW-E022 project.yaml 字段不完整或类型错误

> **生成物，请勿手改**：本页由 `scripts/gen-error-docs.ts` 从 `src/app/errors/registry.ts` 生成；修改文案请改注册表后运行 `npm run gen:errors`。CI 的 `npm run lint:errors` 会拦截漂移与手改。

| 项目 | 内容 |
| --- | --- |
| 段位 | SW-E02x 状态 / 版本 |
| 发生了什么 | project.yaml 字段不完整或类型错误 |
| 原因 | {issues}。 |
| 怎么办 | 按「原因」中的提示逐项修正字段；或从 git 历史恢复该文件（`git checkout -- project.yaml`）。 |
| 退出码 | 1（SPEC-03-EXT：运行期错误） |

文案中的 `{key}` 为运行时上下文占位符，输出时由实际值替换。

## 示例输出

```text
✖ SW-E022 project.yaml 字段不完整或类型错误
  原因：title 必须是非空字符串。
  怎么办：按「原因」中的提示逐项修正字段；或从 git 历史恢复该文件（`git checkout -- project.yaml`）。
  详情：https://github.com/Dawan2/script-writer/blob/main/docs/errors/SW-E022.md
```
