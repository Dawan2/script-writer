# SW-E040 模型调用失败（{model}，已尝试 {attempts} 次）

> **生成物，请勿手改**：本页由 `scripts/gen-error-docs.ts` 从 `src/app/errors/registry.ts` 生成；修改文案请改注册表后运行 `npm run gen:errors`。CI 的 `npm run lint:errors` 会拦截漂移与手改。

| 项目 | 内容 |
| --- | --- |
| 段位 | SW-E04x AI 供应商 |
| 发生了什么 | 模型调用失败（{model}，已尝试 {attempts} 次） |
| 原因 | 模型供应商持续不可用或返回了不可重试的错误：{lastError}。已按 F3 策略完成退避重试与备用模型切换（若已配置）。 |
| 怎么办 | 稍后重试；若持续失败，检查 SW_LLM_BASE_URL / SW_LLM_MODEL 配置与供应商服务状态。凭据相关问题运行 `sw doctor` 核对。 |
| 退出码 | 1（SPEC-03-EXT：运行期错误） |

文案中的 `{key}` 为运行时上下文占位符，输出时由实际值替换。

## 示例输出

```text
✖ SW-E040 模型调用失败（gpt-4o-mini，已尝试 4 次）
  原因：模型供应商持续不可用或返回了不可重试的错误：HTTP 500：Internal Server Error。已按 F3 策略完成退避重试与备用模型切换（若已配置）。
  怎么办：稍后重试；若持续失败，检查 SW_LLM_BASE_URL / SW_LLM_MODEL 配置与供应商服务状态。凭据相关问题运行 `sw doctor` 核对。
  详情：https://github.com/Dawan2/script-writer/blob/main/docs/errors/SW-E040.md
```
