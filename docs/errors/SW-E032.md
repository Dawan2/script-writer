# SW-E032 场编号不合法：{sceneId}

> **生成物，请勿手改**：本页由 `scripts/gen-error-docs.ts` 从 `src/app/errors/registry.ts` 生成；修改文案请改注册表后运行 `npm run gen:errors`。CI 的 `npm run lint:errors` 会拦截漂移与手改。

| 项目 | 内容 |
| --- | --- |
| 段位 | SW-E03x 输入校验 |
| 发生了什么 | 场编号不合法：{sceneId} |
| 原因 | 场编号须是数字（1–3 位会自动补零，如 10 ≡ 010；也接受 4 位及以上），收到的是「{sceneId}」。 |
| 怎么办 | 改用数字编号，如 `sw draft 010`；现有场编号：{existingIds}。 |
| 退出码 | 1（SPEC-03-EXT：运行期错误） |

文案中的 `{key}` 为运行时上下文占位符，输出时由实际值替换。

## 示例输出

```text
✖ SW-E032 场编号不合法：abc
  原因：场编号须是数字（1–3 位会自动补零，如 10 ≡ 010；也接受 4 位及以上），收到的是「abc」。
  怎么办：改用数字编号，如 `sw draft 010`；现有场编号：010、020。
  详情：https://github.com/Dawan2/script-writer/blob/main/docs/errors/SW-E032.md
```
