# 关键原则
1. 生成的文档型内容，尽量使用中文；
2. 执行工作前，先参照第一性原理对任务进行拆解，再进行工作；
3. 所有界面上显示的内容（按钮、标题、提示等），都要从用户视角出发进行设计和编写，不能从 工程、AI、开发者视角出发，不要显示对终端用户无用的内容；
4. 在 Agent、Skill 中的内容，描述要尽可能的精简，并且不要无脑进行内容的增加，但不要为了缩字而吞掉一个完整句子中的文字，不要凭空造词，有必要的话，可以对 skill 的内容进行整体重构，而不是在屎山上继续堆积，也不要把个例的内容直接当做 few shot 进行补充；
5. DO NOT send optional commentary；
6. 进行 Agent 相关的调整后，要确定调整内容在 Skill 中落地，对应的验收规则，在 skill 完成时、内容质量校验时、最后的 AI 审稿时，是一致的；
7. 当用户提供 sessionID ，你需要定位问题时，阅读“tools/zdebug/README.md”，根据其中的说明，进行问题定位。
8.  创建或修改 Agent、Skill 相关内容时，需提前阅读`docs/Agent设计规范.md`，并遵循其中的规范；